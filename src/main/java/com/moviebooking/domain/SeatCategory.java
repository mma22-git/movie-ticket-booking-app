package com.moviebooking.domain;

/**
 * Seat tiers within a theater. Pricing per tier is introduced in a later phase;
 * for now the category is purely descriptive.
 */
public enum SeatCategory {
    SILVER,
    GOLD,
    PLATINUM
}
